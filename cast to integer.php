<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <?php
    $a = 10; //this is static variable
    $b = 5.24;
    $c = "25kiloram";
    $d = "hello world";
    $e =  "hello world 25kiloram";
    $f =  true;
    $g =  false;
    $h =  null;

    $a = (int) $a; //this is static variable
    $b = (int) $b; //this is static variable
    $c = (int) $c; //this is static variable
    $d = (int) $d; //this is static variable
    $e = (int) $e; //this is static variable
    $f = (int) $f; //this is static variable
    $g = (int) $g; //this is static variable
    $h = (int) $h; //this is static variable

var_dump($a); //this is static variable

var_dump($b); //this is static variable

var_dump($c); //this is static variable

var_dump($d); //this is static variable

var_dump($e); //this is static variable

var_dump($f); //this is static variable

var_dump($g); //this is static variable

var_dump($h); //this is static variable
     ?>
</body>
</html>